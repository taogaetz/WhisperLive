name: Pascal image

on:
  pull_request:
    paths:
      - ".github/workflows/pascal-image.yml"
      - "docker/Dockerfile.pascal"
      - "requirements/pascal.*"
      - "tests/**"
      - "whisper_live/**"
  push:
    branches:
      - codex/pascal-sm61
    tags:
      - "pascal-*"
  workflow_dispatch:

permissions:
  contents: read
  packages: write

env:
  IMAGE: ghcr.io/${{ github.repository_owner }}/whisperlive

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/build-push-action@v6
        with:
          context: .
          file: docker/Dockerfile.pascal
          target: test
          load: true
          tags: whisperlive:pascal-test
          cache-from: type=gha
          cache-to: type=gha,mode=max
      - run: docker run --rm whisperlive:pascal-test

  publish:
    if: github.event_name != 'pull_request'
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/metadata-action@v5
        id: metadata
        with:
          images: ${{ env.IMAGE }}
          tags: |
            type=raw,value=1080ti
            type=sha,prefix=pascal-
      - uses: docker/build-push-action@v6
        with:
          context: .
          file: docker/Dockerfile.pascal
          target: runtime
          platforms: linux/amd64
          push: true
          tags: ${{ steps.metadata.outputs.tags }}
          labels: ${{ steps.metadata.outputs.labels }}
          cache-from: type=gha
          provenance: mode=max
          sbom: true
